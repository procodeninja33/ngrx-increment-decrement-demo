import { createAction } from '@ngrx/store';

export const increment = createAction('[Counter] iasdasnc');
export const decrement = createAction('[Counter] dasdsa');
export const reset = createAction('[Counter] Reset');